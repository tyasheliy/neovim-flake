require("telescope").setup({
	defaults = {--require("telescope.themes").get_dropdown {
		layout_strategy = "vertical",
		layout_config = {
      			vertical = {
        			preview_cutoff = 0,
      			},
    		},
	}
})

