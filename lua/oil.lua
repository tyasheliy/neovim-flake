require("oil").setup({
	default_file_explorer = false,
	use_default_keymaps = false,
	columns = {
    	"icon",
    	"size",
    -- "permissions",
    -- "mtime",
  	},
	view_options = {
		show_hidden = true,
		case_insensitive = true,
	}
})
